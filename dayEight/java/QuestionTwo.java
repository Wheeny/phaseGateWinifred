//import java.util.Arrays;
public class QuestionTwo{

    public static void main(String[] args){
    String[] symbols = {],(,),[};
    
    System.out.println(inverseCheck(symbols));
    
    }

    public static boolean inverseCheck(String[] symbols){
        String[] inverseSymbols = new String[symbols.length];
        int index = 0;
        
        for(int count = 0; count < symbols.length(); count++){
            inverseSymbols.charAt[index] = symbols.charAt[index].inverse;
            index++;
        }
        
        for(int count = 0; count < symbols.length(); count++){
            if(inverseSymbols.charAt[index] = symbols.charAt[index]){
            
            }
        }
        
    return true;
    }

}

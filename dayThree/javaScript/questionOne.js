let word = "Happy"; 
let reverse = "";

for(let count = word.length - 1; count >= 0; count--) {
        
            reverse = reverse + word.charAt(count);
        }
        
      console.log(reverse);
    

